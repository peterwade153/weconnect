
language: python

python:
  - "3.4"


install:
  - pip install coverage
  - pip install coveralls
  - pip install pytest-cov
  - pip install -r requirements.txt

script:
  - pytest --cov api/v1/tests/

after_success:
  - coverage report
  - coveralls
   #uploads reports to  coveralls.io